import 'package:flutter/material.dart';

/// Draw & Guess screen placeholder.
class DrawGuessScreen extends StatelessWidget {
  const DrawGuessScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Draw & Guess'));
  }
}
