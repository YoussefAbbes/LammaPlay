import 'package:flutter/material.dart';

/// App color tokens inspired by Sporcle-like palette.
class LmColors {
  static const primary = Color(0xFFF59E0B); // amber-500
  static const primaryDark = Color(0xFFB45309);
  static const secondary = Color(0xFF0EA5E9); // sky-500
  static const secondaryDark = Color(0xFF0369A1);
  static const accent = Color(0xFF22C55E); // green-500

  static const surfaceLight = Color(0xFFFFFFFF);
  static const surfaceDark = Color(0xFF0B1220);

  static const textPrimaryLight = Color(0xFF0F172A);
  static const textPrimaryDark = Color(0xFFE2E8F0);

  static const warning = Color(0xFFF97316);
  static const error = Color(0xFFEF4444);
  static const success = Color(0xFF10B981);
}
